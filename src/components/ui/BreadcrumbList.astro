---
const { className = '', ...rest } = Astro.props
---
<ol
  data-slot="breadcrumb-list"
  class={`text-muted-foreground flex flex-wrap items-center gap-1.5 text-sm break-words ${className}`}
  {...rest}
>
  <slot />
</ol>
